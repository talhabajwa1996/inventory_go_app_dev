import 'package:flutter/material.dart';

class CountUI extends StatefulWidget {
  const CountUI({Key? key}) : super(key: key);

  @override
  State<CountUI> createState() => _CountUIState();
}

class _CountUIState extends State<CountUI> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
